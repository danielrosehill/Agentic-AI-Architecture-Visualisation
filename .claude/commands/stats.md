Show statistics about the current architecture data model.

Read `data/architecture.json` and report:

- Total layers, nodes, connections
- Nodes per layer
- Connections per layer (as source)
- Orphan nodes (zero connections)
- Connection density (connections / possible connections)
- Invocation pattern distribution (autonomous, deterministic, bidirectional, transport, unset)
- Position distribution (left, center, right column counts)
- Style distribution (solid, dashed, checkpoint)
- Nodes with longest/shortest descriptions
