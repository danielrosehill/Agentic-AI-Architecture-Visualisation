Add a new connection between nodes in the architecture.

User description: $ARGUMENTS

Read `data/architecture.json`. Based on the user's description:

1. Identify the `from` and `to` node/layer IDs
2. Verify both IDs exist
3. Choose appropriate style, color (match source layer), and invocation_pattern
4. Add the connection to `data/architecture.json`
5. Update `docs/architecture.md`
6. Report what was added
