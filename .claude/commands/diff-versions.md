Compare two versions of the architecture data model.

Versions to compare: $ARGUMENTS (e.g., "v1 v2")

1. Read the two version files from `context/versions/`
2. Compare layers: added, removed, modified
3. Compare nodes: added, removed, modified
4. Compare connections: added, removed, modified
5. Summarize the differences in a clear table format
