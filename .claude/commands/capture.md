Capture a design note or architectural insight into the context store.

Note to capture: $ARGUMENTS

1. Read existing `context/` files to check for duplication
2. Clean up and format the note (fix typos, add structure, preserve the insight)
3. Determine the best file:
   - Extends existing topic → append to that file
   - New gap/improvement → `context/review-gaps.md`
   - New value route → `context/value-routes.md`
   - New topic → create new file in `context/`
4. If the note implies data model changes, add an "Implications" section
5. Report what was captured and where
